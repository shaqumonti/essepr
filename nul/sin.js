if (result.status === 'timeout') {
  // Handle the timeout scenario
  console.log('The operation timed out.');
  // You can perform additional operations here, such as retrying the request or alerting the user
} else {
  // Handle other statuses
}
